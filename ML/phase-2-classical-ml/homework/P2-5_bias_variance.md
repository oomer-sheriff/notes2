# Task P2-5: Bias-Variance Tradeoff Experiment

**Goal:** Visually demonstrate underfitting and overfitting by fitting polynomials of increasing complexity to noisy data.

Create a Jupyter Notebook in `homework/lab-files/`.

### 1. Data Generation
Generate a non-linear dataset with noise:
```python
import numpy as np
import matplotlib.pyplot as plt

np.random.seed(42)
X = np.sort(np.random.rand(40, 1) * 10, axis=0)
y = np.sin(X).ravel() + np.random.randn(40) * 0.5 # Sine wave + noise

# Split into train/test
from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=42)
```

### 2. Polynomial Fitting
Use `PolynomialFeatures` combined with `LinearRegression` (via a `Pipeline`) to fit models of degree 1, 3, 5, 10, and 20.

```python
from sklearn.preprocessing import PolynomialFeatures
from sklearn.linear_model import LinearRegression
from sklearn.pipeline import make_pipeline
from sklearn.metrics import mean_squared_error

degrees = [1, 3, 5, 10, 20]
train_errors = []
test_errors = []

# Loop through the degrees, train the pipeline, and record the train/test MSE.
```

### 3. The Visualizations
1. **The Fits:** Create a large matplotlib figure. Plot the original scatter data. Then, plot the line generated by each of the polynomial models.
   *Observe:* Degree 1 is a straight line (High Bias / Underfitting). Degree 20 wildly wiggles to hit every single training point (High Variance / Overfitting).
2. **The Tradeoff Curve:** Create a second plot. On the x-axis put the `degrees`. On the y-axis, plot the `train_errors` line and the `test_errors` line.
   *Observe:* As complexity (degree) increases, training error goes down to zero. Test error goes down initially, hits a minimum, and then skyrockets as the model overfits.

*Question:* Based on your curve, what is the optimal polynomial degree for this dataset?
